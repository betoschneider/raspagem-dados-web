version: '3.8'

services:
  myspeed:
    build: .
    container_name: select_myspeed_container
    restart: always # Isso reinicia o container automaticamente se ele falhar
    volumes:
      - /caminho/para/o/sqlite.db:/DATA/AppData/myspeed/data/storage.db
    environment:
      - TZ=America/Sao_Paulo
